How to run the sample app:

1. Get latest version of swagger projects using
        git clone git@github.com:wordnik/swagger-core.git
        git clone git@github.com:wordnik/swagger-ui.git
        git clone git@github.com:wordnik/swagger-code-gen.git
        git clone git@github.com:wordnik/swagger-sample-app.git

2. Set swagger_ui_path in build.xml. If swagger-ui and swagger-sample-app are at same level of directory structure, we need not set this value. 

3. run "ant deploy server.start"

4. Access the url http://localhost:8002/swagger-sample-app/index.html