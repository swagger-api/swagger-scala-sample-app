How to run the sample app:

1. Get latest version of swagger projects using
        git clone git@github.com:wordnik/swagger-core.git
        git clone git@github.com:wordnik/swagger-ui.git
        git clone git@github.com:wordnik/swagger-code-gen.git
        git clone git@github.com:wordnik/swagger-sample-app.git

2. run "ant deploy server.start"

3. Make sure to add package name "com.wordnik.swagger.core" to web.xml servlet init parameter "com.sun.jersey.config.property.packages"

4. Go to swagger-ui project and opn the index.html

5. Enter URL for the resource as "http://localhost:8002/api" and api-key as "sample-app-un-secure-key" for accessing unsecured API and "sample-app-secure-key" for accessing secure APIs


